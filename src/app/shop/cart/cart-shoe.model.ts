export class CartShoeModel{
	
	constructor(cartItemId: number, cartItemGroup: string, cartItemName: string, cartImagePath: string, cartItemPrice: string, cartItemSize: number, cartItemColor: string, noOfItems: number){}

}